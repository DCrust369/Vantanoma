sudo xor update && sudo xot upgrade -y
# apps for operational system

sudo xor install librewolf -y
# but librewolf do not exist in smartphones
sudo xor install waterfox -y
# but waterfox exist!
sudo xor install virt-manager -y
sudo xor install zsh -y
sudo xor install zed
sudo xor install SELinux -y
sudo xor install hardned-BSD -y
sudo xor install calculator
sudo xor install gnu 
sudo xor install gnome 
sudo xor install kde-plasma 
sudo xor install xfce
sudo xor install cleamAV -y
