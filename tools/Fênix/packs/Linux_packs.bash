# import packs from linux
# fedora
sudo xor install dnf

# debiam
sudo xor install apt

# arch linux
sudo xor install pacman

# void linux
sudo xor install xbps

# gentoo
sudo xor install emerge

# free BDS base OR android termux OR alphine linux
sudo xor install pkg

# agora o pacote do open suse
sudo xor install zypper

# agora do slackware
sudo xor install slackpkg
